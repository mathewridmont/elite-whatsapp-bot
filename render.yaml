services:
  - type: web
    name: elite-class-v10
    runtime: docker
    plan: starter
    healthCheckPath: /health
    disk:
      name: elite-class-data
      mountPath: /app/storage
      sizeGB: 1
