services:
  - type: web
    name: elite-whatsapp-bot
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    disk:
      name: whatsapp-data
      mountPath: /app/storage
      sizeGB: 1
