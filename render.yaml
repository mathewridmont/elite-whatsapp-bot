services:
  - type: web
    name: elite-whatsapp-bot
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "10000"
    disk:
      name: whatsapp-data
      mountPath: /app/storage
      sizeGB: 1
